## 📋 Проект
Данный проект содержит реализацию и сравнение по времени двух алгоритмов сортировки:
- **Сортировка расчёской (Comb Sort)**
- **Быстрая сортировка (Quicksort)** 

Проект разработан в рамках курсовой работы для эмпирического сравнения производительности этих алгоритмов на массивах различного размера.

Измеряется среднее время выполнения каждого алгоритма на случайных массивах разного размера.

---

## 📌 Описание

Программа:
- Генерирует случайные массивы целых чисел размерами [10, 20, 50, 100, 200, 500, 1000, 2000, 5000, 10000]
- Сортирует их с помощью Comb Sort и Quick Sort
- Выполняет 10 000 тестов для каждого размера массива
- Вычисляет среднее время выполнения каждого алгоритма

---
## 🏗️ Структура кода

### Основные функции:
- `comb_sort(array)` — реализация сортировки расчёской
- `quicksort(array)` — реализация быстрой сортировки

### Тестирование:
- Тестирование на массивах размером от 10 до 10 000 элементов
- Для каждого размера выполняется 10 000 итераций
- Используются случайные массивы с целыми числами от -100 000 до 100 000
- Измерение времени выполнения с помощью `time.perf_counter()`

---

## ⚙️ Используемые технологии

- Python 3
- Модули:
  - `random`
  - `time`

---

## 🚀 Как запустить

1. Убедитесь, что у вас установлен Python 3  
2. Склонируйте репозиторий или скачайте файл
3. Запустите программу командой:

```bash
combsort-quicksort.py
